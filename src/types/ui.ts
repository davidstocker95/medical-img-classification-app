/**
 * Represents 2D coordinates for positioning UI elements.
 */
export interface XYPosition {
  x: number;
  y: number;
}

/**
 * Represents dimensions (width and height) of a UI element.
 */
export interface BoxDimensions {
  width: number;
  height: number;
}
